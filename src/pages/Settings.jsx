import EmptyState from '../components/ui/EmptyState'

export default function Settings() {
  return <EmptyState icon="⚙️" title="Settings" subtitle="Manage users, integrations, and workspace configuration." />
}
